export const colors = {
    black: '#000e1a',
    white: '#fff',
    blue: '#007ce0',
    navy: '#004175'
};

const breakpoints = ['52em', '72em'];

breakpoints.sm = breakpoints[0];
breakpoints.md = breakpoints[1];

export const space = [0, 4, 8, 16, 32, 64, 128, 256, 512];

export default {
    colors,
    space,
    breakpoints,
};
